<script webc:type="js">
    const iconSize = size || "text-3xl";
    if (!name) {
        throw new Error('<tree-icon> is missing the `name` prop')
    }

    `<span class="icon icon--${$data.icon.theme} icon--${$data.icon.theme}--${name} ${iconSize}" aria-hidden="true">&nbsp;</span>`;
</script>